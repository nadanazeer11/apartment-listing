export type Apartment = {
  _id: string;
  name: string;
  unitNumber: string;
  project?: string;
  price: number;
  description?: string;
  images?: string[];
  createdAt: string;
  updatedAt: string;
};
